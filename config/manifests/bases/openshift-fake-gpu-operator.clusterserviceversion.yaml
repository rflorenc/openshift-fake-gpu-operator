apiVersion: operators.coreos.com/v1alpha1
kind: ClusterServiceVersion
metadata:
  annotations:
    alm-examples: '[]'
    capabilities: Basic Install
    categories: AI/Machine Learning,Developer Tools
    containerImage: quay.io/rlourencc/openshift-fake-gpu-operator:v0.1.0
    createdAt: "2026-06-23T15:00:00Z"
    description: Simulate NVIDIA GPUs on CPU-only OpenShift nodes for testing and
      development.
    repository: https://github.com/rflorenc/openshift-fake-gpu-operator
    support: Community
  name: openshift-fake-gpu-operator.v0.0.0
  namespace: placeholder
spec:
  apiservicedefinitions: {}
  customresourcedefinitions:
    owned:
    - displayName: Fake GPUConfig
      kind: FakeGPUConfig
      name: fakegpuconfigs.gpu.openshift.io
      version: v1alpha1
  description: |
    ## OpenShift Fake GPU Operator

    Simulates NVIDIA GPU resources on CPU-only OpenShift nodes, enabling development
    and testing of GPU-dependent workloads without physical hardware.

    ### Features

    - Simulates full GPUs and MIG partitions on any worker node
    - Supports 7 GPU profiles: A100, H100, H200, B200, GB200, L40S, T4
    - Registers `nvidia.com/gpu` and MIG slice resources with kubelet
    - Exports Prometheus-compatible DCGM metrics
    - Declarative CRD-driven configuration via `FakeGPUConfig`
    - Automatic node labeling and topology management
    - Detects and prevents conflicts with Helm-installed fake-gpu-operator

    ### Use Cases

    - Testing Kueue GPU quota and scheduling without GPUs
    - Developing RHOAI workbenches and pipelines in non-GPU environments
    - CI/CD pipeline validation for GPU workloads
    - Training and workshop environments

    ### Quick Start

    1. Install the operator from OperatorHub
    2. Create a `FakeGPUConfig` resource targeting your worker nodes
    3. GPU resources appear on nodes automatically

    ```yaml
    apiVersion: gpu.openshift.io/v1alpha1
    kind: FakeGPUConfig
    metadata:
      name: training-pool
    spec:
      gpuProfile: h200
      gpuCount: 8
      nodeSelector:
        node-role.kubernetes.io/worker: ""
    ```
  displayName: OpenShift Fake GPU Operator
  icon:
  - base64data: iVBORw0KGgoAAAANSUhEUgAAADAAAAAwCAIAAADYYG7QAAAAQElEQVR4nO3OQQ0AIAwAsanGG+7mguPRpAI6585X+oGQkJBQPRASEhKqB0JCQkL1QEhISKgeCAkJCdUDISGhxxbl9qeX0m/NCwAAAABJRU5ErkJggg==
    mediatype: image/png
  install:
    spec:
      deployments: []
    strategy: deployment
  installModes:
  - supported: true
    type: OwnNamespace
  - supported: true
    type: SingleNamespace
  - supported: true
    type: AllNamespaces
  - supported: false
    type: MultiNamespace
  keywords:
  - gpu
  - nvidia
  - simulation
  - fake-gpu
  - kueue
  - ai
  - machine-learning
  - testing
  links:
  - name: Source Code
    url: https://github.com/rflorenc/openshift-fake-gpu-operator
  - name: Fake GPU Operator (upstream)
    url: https://github.com/run-ai/fake-gpu-operator
  maintainers:
  - email: rlourenco@redhat.com
    name: Ricardo Lourenco
  maturity: alpha
  minKubeVersion: 1.28.0
  provider:
    name: Community
  version: 0.0.0
